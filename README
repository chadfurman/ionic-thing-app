# Thing App for Oauth API
+ [deploy api](https://github.com/shadelotus/oauth-trials-and-tribulations)
+ npm install
+ bower install
+ gulp watch

### note
The user id is hardcoded to 1 for the lists endpoint
You'll need an access token from the API for the web-based version
The mobile app gets the token via oauth
